name: Shell CI

on:
  push:
    branches: [ "main", "master", "ft**", "fb**" ]
    paths:
      - 'FrontEnd/shell/**'
      - '.github/workflows/frontend-shell-ci.yml'
  pull_request:
    branches: [ "main", "master", "ft**", "fb**" ]
    paths:
      - 'FrontEnd/shell/**'
      - '.github/workflows/frontend-shell-ci.yml'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: ./FrontEnd/shell/package-lock.json

    - name: Install Dependencies
      working-directory: ./FrontEnd/shell
      run: npm ci

    - name: Run NPM Audit for Vulnerabilities
      working-directory: ./FrontEnd/shell
      run: npm audit --audit-level=high || echo "Audit found vulnerabilities, check logs."

    - name: Run Linter
      working-directory: ./FrontEnd/shell
      run: npm run lint --if-present

    - name: Run Tests
      working-directory: ./FrontEnd/shell
      run: npm run test --if-present

    - name: Build
      working-directory: ./FrontEnd/shell
      run: npm run build --if-present
