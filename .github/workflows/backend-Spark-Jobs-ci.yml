name: Spark Jobs CI

on:
  push:
    branches: [ "main", "master", "ft**", "fb**" ]
    paths:
      - 'Processors/Spark-Jobs/**'
      - 'Infra/**'
      - '.github/workflows/backend-Spark-Jobs-ci.yml'
  pull_request:
    branches: [ "main", "master", "ft**", "fb**" ]
    paths:
      - 'Processors/Spark-Jobs/**'
      - 'Infra/**'
      - '.github/workflows/backend-Spark-Jobs-ci.yml'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Start Infrastructure
      working-directory: ./Infra
      run: |
        docker compose up -d
        sleep 45 # Wait for services to initialize

    - name: Build and Test
      working-directory: ./Processors/Spark-Jobs
      run: mvn -B clean verify

    - name: Verify Docker Build
      if: hashFiles('Processors/Spark-Jobs/Dockerfile') != ''
      working-directory: ./Processors/Spark-Jobs
      run: docker build -t spark-jobs:test .

    - name: Stop Infrastructure
      if: always()
      working-directory: ./Infra
      run: docker compose down -v
