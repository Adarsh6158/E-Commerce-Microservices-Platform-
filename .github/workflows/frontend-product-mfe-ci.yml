name: Product Mfe CI

on:
  push:
    branches: [ "main", "master", "ft**", "fb**" ]
    paths:
      - 'FrontEnd/product-mfe/**'
      - '.github/workflows/frontend-product-mfe-ci.yml'
  pull_request:
    branches: [ "main", "master", "ft**", "fb**" ]
    paths:
      - 'FrontEnd/product-mfe/**'
      - '.github/workflows/frontend-product-mfe-ci.yml'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: ./FrontEnd/product-mfe/package-lock.json

    - name: Install Dependencies
      working-directory: ./FrontEnd/product-mfe
      run: npm ci

    - name: Run NPM Audit for Vulnerabilities
      working-directory: ./FrontEnd/product-mfe
      run: npm audit --audit-level=high || echo "Audit found vulnerabilities, check logs."

    - name: Run Linter
      working-directory: ./FrontEnd/product-mfe
      run: npm run lint --if-present

    - name: Run Tests
      working-directory: ./FrontEnd/product-mfe
      run: npm run test --if-present

    - name: Build
      working-directory: ./FrontEnd/product-mfe
      run: npm run build --if-present
