name: Search Service CI

on:
  push:
    branches: [ "main", "master", "ft**", "fb**" ]
    paths:
      - 'Backend/search-service/**'
      - 'Infra/**'
      - '.github/workflows/backend-search-service-ci.yml'
  pull_request:
    branches: [ "main", "master", "ft**", "fb**" ]
    paths:
      - 'Backend/search-service/**'
      - 'Infra/**'
      - '.github/workflows/backend-search-service-ci.yml'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Start Infrastructure
      working-directory: ./Infra
      run: |
        docker compose up -d
        sleep 45 # Wait for services to initialize

    - name: Build and Test
      working-directory: ./Backend/search-service
      run: mvn -B clean verify

    - name: Verify Docker Build
      if: hashFiles('Backend/search-service/Dockerfile') != ''
      working-directory: ./Backend/search-service
      run: docker build -t search-service:test .

    - name: Stop Infrastructure
      if: always()
      working-directory: ./Infra
      run: docker compose down -v
