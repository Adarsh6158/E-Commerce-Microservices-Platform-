apiVersion: v1
kind: Service
metadata:
  name: pricing-service
  namespace: ecommerce
  labels:
    app: pricing-service
spec:
  type: ClusterIP
  selector:
    app: pricing-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8085
